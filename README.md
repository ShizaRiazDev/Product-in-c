# Product-in-c
This program allows the user to input two integers and calculates their product using a separate multiply() function.  It demonstrates:  Using functions to perform operations  Passing parameters to functions  Taking user input with scanf()  Returning computed values  Printing results to the screen
